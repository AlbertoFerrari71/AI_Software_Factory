# safety module

Modulo futuro per policy engine, approval model, dry-run engine e classificazione azioni.

Nello STEP 030 non contiene ancora logica applicativa. La policy operativa vive in:

- `docs/05_SECURITY_MODEL.md`
- `policies/safety_policy.v0.json`
- `policies/path_policy.v0.json`

La futura implementazione dovrà leggere la policy versionata invece di duplicare regole hard-coded.
